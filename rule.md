# 斗地主牌型规则与实现说明

本文档描述当前工程内**已实现的全部牌型**，以及与实现一一对应的代码位置、判定顺序与比牌逻辑。

**核心脚本**：`scripts/ddz_rules.gd`（`classify` / `same_pattern_kind` / `beats`）  
**点力函数**：`scripts/card_defs.gd` 中的 `ddz_rank_value(card_id)`，出牌组合按**点力 multiset** 判定。

---

## 1. 点力（`ddz_rank_value`）

| 点力值 | 牌面 |
|--------|------|
| 0～7 | 3～10 |
| 8 | J |
| 9 | Q |
| 10 | K |
| 11 | A |
| 12 | 2 |
| 13 | 小王 |
| 14 | 大王 |

顺子、连对、飞机（纯/带翅）的「连续」段**不允许**包含点力 12（2）与 13、14（王牌），实现上体现为：参与连续段的点力需在 **0～11**（3～A）范围内。

---

## 2. 牌型枚举 `Kind`（`ddz_rules.gd`）

每种合法出牌在 `classify` 中返回字典，至少包含：

- **`kind`**：`Kind` 枚举值  
- **`main`**：比大小时主要比较的点力（或固定值）  
- **`extra`**：张数、长度等辅助信息；同类牌是否「同一牌型」由 `same_pattern_kind` 比较 `kind` + `extra` 是否完全一致  

---

## 3. 各牌型定义与实现方式

### 3.1 `PASS`（过）

- **条件**：选中牌数为 0。  
- **返回**：`{"kind": PASS, "main": -1, "extra": null}`。

---

### 3.2 `ROCKET`（王炸）

- **条件**：恰好 2 张，且为小王+大王（`card_id` 为 52 与 53，顺序不限）。  
- **返回**：`main: 14`，`extra: null`。  
- **实现**：在统计 `_rank_counts` 之前单独判断 `n == 2`。

---

### 3.3 `SINGLE`（单张）

- **条件**：1 张牌。  
- **`main`**：该牌的 `ddz_rank_value`。

---

### 3.4 `PAIR`（对子）

- **条件**：2 张，同点力。  
- **`main`**：该点力。

---

### 3.5 `TRIPLE`（三张）

- **条件**：3 张，同点力。  
- **`main`**：该点力。

---

### 3.6 `BOMB`（炸弹）

- **条件**：4 张，同点力。  
- **`main`**：四张的点力。

---

### 3.7 `TRIPLE_WITH_SINGLE`（三带一）

- **条件**：4 张，点力 multiset 为「一个 3 张 + 一个 1 张」。  
- **`main`**：三张的点力。  
- **`extra`**：固定为 `4`（张数，便于扩展/显示）。  
- **实现**：`_is_triple_single`。

---

### 3.8 `TRIPLE_WITH_PAIR`（三带二）

- **条件**：5 张，点力 multiset 为「一个 3 张 + 一个对子」。  
- **`main`**：三张的点力。  
- **`extra`**：固定为 `5`。  
- **实现**：`_is_triple_pair`。

---

### 3.9 `STRAIGHT`（顺子）

- **条件**：至少 5 张；每种点力恰好 1 张；点力连续；**不能**包含 2（12）与王牌（≥13）。  
- **`main`**：顺子中**最大**的点力（排序后取最大键）。  
- **`extra`**：张数 `n`。  
- **实现**：`_is_straight(counts, n)`；在 `n == 5` 与 `n >= 5` 两处尝试（与 5 张顺、更长顺兼容）。

---

### 3.10 `PAIR_STRAIGHT`（连对）

- **条件**：至少 6 张且为偶数；每种点力恰好 2 张；至少 3 个连续点力对；**不能**包含 2 与王牌。  
- **`main`**：连对中**最大**点力。  
- **`extra`**：总张数。  
- **实现**：`_is_pair_straight`、`_pair_straight_ranks`。

---

### 3.11 `FOUR_WITH_TWO`（四带二）

两种结构，且均需通过 `_four_kickers_no_joker_bomb`：

| 张数 | 结构 | `extra` |
|------|------|---------|
| 6 | 四张同点 + 两个不同点力的单张 | `6` |
| 8 | 四张同点 + 两个对子（两个不同点力） | `8` |

- **`main`**：四张同点的点力（`_four_rank_in`）。  
- **所带牌限制（实现）**：所带点力**不能**为小王/大王（13、14）；所带部分**不能**出现 4 张同点（视为炸弹）；四张主体点力不参与上述「所带」检查。  
- **实现**：`_is_four_two_singles` / `_is_four_two_pairs` + `_four_kickers_no_joker_bomb`。  
- **分类顺序**：在 `classify` 中于**飞机类之前**判断，避免张数与其它复合牌型混淆。

---

### 3.12 `PLANE`（飞机，纯连续三张）

- **条件**：  
  - 总张数 `n = 3 * k`，`k >= 2`；  
  - 恰好 `k` 种点力，每种**恰好 3 张**；  
  - 这 `k` 种点力为**连续整数**，且均在 **0～11**（3～A），即不含 2、不含王。  
- **`main`**：这段连续三张中**最大**点力（`st + k - 1`）。  
- **`extra`**：连三长度 `k`。  
- **实现**：`_try_pure_plane`（枚举起点 `st`）。

---

### 3.13 `PLANE_WITH_WINGS`（飞机带翅膀）

- **结构**：  
  - 连续 `k` 组三张（`k >= 2`），每组点力在 **0～11**，且飞机段上每种点力**恰好用满 3 张**（不能有第 4 张留在飞机点力上）；  
  - 另有 **k 组翅膀**：每组为 **1 张单牌** 或 **1 对**，翅膀点力彼此区分且在实现上表现为剩余 multiset 中每个非飞机点力张数为 1 或 2；  
  - 翅膀**不能**含王牌（点力 ≥13）；剩余牌不能出现除飞机已用外的「炸弹」（即剩余中任一点力张数不能为 3 或 4——实现里要求剩余仅 1 或 2 张/点力）。  
- **张数关系**：记「对子翅膀」个数为 `p`（`0 ≤ p ≤ k`），则总张数 `n = 4k + p`（等价于 `n` 在 `[4k, 5k]` 内）。  
- **`main`**：飞机连续段的最大点力。  
- **`extra`**：编码为 `(k << 5) | num_pair_wings`，即高位存 `k`，低位存对子翅膀数 `num_pair_wings`。比牌时与 `same_pattern_kind` 一致：须 `kind`、`extra` 皆相同才比 `main`。  
- **实现**：`_try_plane_with_wings` 枚举 `k` 与起点 `st`，`_match_plane_wings_at` 做扣减与翅膀校验。  
- **分类顺序**：在纯飞机之后；若与别类冲突，以先匹配到的分支为准（当前顺序下四带二已先处理 6、8 张）。

---

## 4. `classify` 判定顺序（概要）

1. 空牌 → `PASS`  
2. 2 张 → 可能 `ROCKET`  
3. `_rank_counts` 后：单、对、三、炸弹（4 张）、三带一（4 张）、5 张顺/三带二  
4. `n >= 5` 顺子  
5. `n >= 6` 且偶数 → 连对  
6. `n == 6` / `8` → 四带二（含所带校验）  
7. 纯飞机 → 飞机带翅膀  
8. 否则 → `INVALID`

---

## 5. 比牌 `beats(last, cur)`

1. `cur` 非法 → 不能出。  
2. 桌面无上一手（`PASS` 或空）→ 任意非 `PASS`、非 `INVALID` 可出。  
3. **`ROCKET`**：可压任意非王炸；王炸仅能被更大…（实现中王炸最大，仅 `ROCKET` 自压场景由规则保证唯一）。  
4. **`BOMB`**：可压所有非炸弹、非王炸；炸弹之间比 `main` 点力。  
5. 非炸弹类：**必须** `same_pattern_kind(last, cur)` 为真（同 `kind` 且 `extra` 相同），再比 `cur.main > last.main`。

---

## 6. 相关代码文件

| 文件 | 作用 |
|------|------|
| `scripts/ddz_rules.gd` | 牌型分类、比牌 |
| `scripts/card_defs.gd` | `ddz_rank_value` 等牌张与点力 |
| `scripts/main.gd` | `_kind_name` 界面展示名、出牌流程 |
| `scripts/ddz_ai.gd` | 跟牌：`_try_same_pattern`、`find_follow` 等 |
| `scripts/ddz_ai_play.gd` | 首家自由出牌启发（`choose_free_lead`） |

---

## 7. 界面展示名（`main.gd` 中 `_kind_name`）

与上述 `Kind` 对应的中文/格式包括：单张、对子、三张、顺子(张数)、炸弹、王炸、三带一、三带二、连对(张数)、四带二(张数)、飞机(N连三)、飞机带翅(N连三·M对翼) 等，具体以 `main.gd` 中 `match` 为准。

---

*文档与当前 `scripts/ddz_rules.gd` 实现同步；若改规则请先改代码再更新本文档。*
